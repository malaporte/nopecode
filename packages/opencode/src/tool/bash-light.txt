Execute a shell command in the persistent session.

- Use `workdir` instead of `cd <dir> && ...`.
- Use this tool for terminal tasks like git, package managers, builds, tests, and scripts.
- Do not use it for file reading, writing, editing, or searching when dedicated tools fit better.
- Quote paths with spaces using double quotes.
- If a command creates files or directories, verify the parent location first.
- Prefer parallel tool calls for independent commands and `&&` for dependent commands.
- Include a short 5-10 word `description`.
- If output is truncated, use read/search tools on the saved output instead of shell truncation commands.
- Do not use destructive or forceful git commands unless the user explicitly requests them.
- Only create commits or push when the user explicitly asks.
